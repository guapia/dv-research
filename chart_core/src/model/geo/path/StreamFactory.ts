namespace android.test.map{
    'use strict';
    export class StreamFactory{
        public static streamLine(coordinates:any[],stream:Stream){}
        public static streamArea(coordinates:any[], stream:Stream){}
    }
}